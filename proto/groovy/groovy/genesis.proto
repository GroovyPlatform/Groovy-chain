syntax = "proto3";
package groovy.groovy;

import "gogoproto/gogo.proto";
import "groovy/groovy/params.proto";

option go_package = "groovy/x/groovy/types";

// GenesisState defines the groovy module's genesis state.
message GenesisState {
  Params params = 1 [(gogoproto.nullable) = false];
}
